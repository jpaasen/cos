from Configurable import Configurable


class Signal(Configurable):
   """TX signal characteristics.""" 
#   def __init__(self):
#      self.mod       = None                  # Modulation ('none','up-chirp','down-chirp',...)
#      self.tau       = None                  # Ping width                         [s]
#      self.prf       = None                  # Pulse repetition frequency [Hz]
#      self.fc         = None                 # Carrier frequency               [Hz]
#      if len(args) != 0:
#      Configurable.__init__(self)
#      Operable.__init__(self)

#   def configure(self, conf):
#      Operable.configure(self, conf)
      
