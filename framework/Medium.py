from Configurable import Configurable
#from Operable import Operable

class Medium(Configurable):
   """The medium represents a potentially a very complex transfer function, and deserves its
   own class. However, for now it will simply act as a container class for relevant parameters."""
#   def __init__(self):
#      self.c       = None                  # Medium propagation speed
      
#      Configurable.__init__(self)
##      Operable.__init__(self, args)
#         
#   def configure(self, conf):
#      Operable.configure(self, conf)
